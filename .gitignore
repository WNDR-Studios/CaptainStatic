# Per-board configuration — copy config.h.example to config.h and set values
anchor/config.h
listener/config.h

# Audio files — too large for git, upload separately via LittleFS image tool
listener/data/track.aac
listener/data/static.aac
